import React from 'react';
import './Button.css';

const Button = () => {
    return (
        <button
            type='button'
        >
            Calcular
        </button>
    );
};

export default Button;